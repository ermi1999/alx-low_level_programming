#include "main.h"
/**
 * mul - will multiply two int
 * a : the first intiger to be multiplied
 * b : the second intiger to be multiplied
 * return : multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
